/**
 * SignaturePad.tsx — stub pour la résolution TypeScript.
 * Metro résout automatiquement SignaturePad.web.tsx (web) et SignaturePad.native.tsx (mobile).
 * Ce fichier existe uniquement pour satisfaire `tsc --noEmit`.
 */
export { SignatureWeb as default } from './SignatureWeb';
